运行前须知：
数据集均来着SPMF，可以自行找到，合成数据集为IBM数据生成器生成；
本作者将数据集都进行了预处理操作，将数据集转为代码支持的形式；
本作者已经将用到的所有数据集以及预处理操作的代码上传。
运行：
选取数据集及更新地址后可以直接运行，每部分功能代码均有注释，输出部分在test中可以自行选择。
*******************************************
Pre-run Instructions
All datasets are from SPMF and can be found on your own; synthetic datasets are generated by the IBM data generator.
The author has preprocessed all datasets and converted them into a format compatible with the code.
The author has uploaded all used datasets and the code for preprocessing operations.
Running the Code
After selecting the dataset and updating the path, you can run the code directly. Each functional section is annotated with comments. You can customize the output content in the test section.
